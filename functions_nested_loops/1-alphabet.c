#include "main.h"
/**
 * main - check the code
 *
 * Return: Always (0) (Success)
 */
int print_alpahbet(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
	{
	_putchar(c);
	}
	_putchar('\n');
	return (0);
}
